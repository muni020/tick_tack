<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Game Interface</title>
  <!-- Include Bootstrap CSS -->
  <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css">
</head>
<body>
  <div class="container">
    <h1 class="text-center text-primary mt-4">Game Interface</h1>
    <div class="d-flex justify-content-between mt-4">
    <%= link_to "Add User", new_user_path, class: "btn btn-primary" %>
    <%= link_to "Start Game", "/user/login", class: "btn btn-warning ml-auto" %>
</div>
    <table class="table table-striped table-bordered mt-4">
      <thead class="thead-dark">
        <tr>
          <th scope="col">#</th>
          <th scope="col">User Name</th>
          <th scope="col">Lost Games</th>
          <th scope="col">Win Games</th>
          <th scope="col">Others</th>
        </tr>
      </thead>
      <tbody>
        <% @users.each_with_index do |user, index| %>
          <tr>
            <td><%= index + 1 %></td>
            <td><%= user.name %></td>
            <td><%= user.lose %></td>
            <td><%= user.win %></td>
            <td>
              <%= link_to "Edit", edit_user_path(user), class: "btn btn-primary"%>
              <%= link_to "Delete", "/users/#{user.id}", data: { turbo_method: :delete, turbo_confirm: "Are you sure?" }, class: "btn btn-outline-danger btn-sm" %>
             </td>
          </tr>
        <% end %>
      </tbody>
    </table>
  </div>

  </body>
</html>
